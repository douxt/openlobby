# Issue 评审报告

**评审范围:** issues/ 目录下 5 条 Phase 1 issue (001-005)
**评审标准:** 04-Issue质量宪法.md (14项) + plan.md rubric
**子进程模型:** opus (deepseek-v4-pro)
**子进程 Session:** feb462d8-c4ba-4378-8e70-ebb26ff565de
**耗时:** 111s

---

## Verdict: CHANGES_REQUESTED

---

## 一、已确认的问题

### 1. [high] issues/005-integration-verification.md:2 — type: AFK 与内容矛盾

**问题:** #005 声明 `type: AFK`（无人机交互），但内容包含 5 个 viewport × 10 项检查 + 11 项桌面回归 + 横屏测试 + 无障碍检查 = 约 70 步手动验证。宪法规则 #2: "AFK 不含人工步骤"。

**核实结论:** 真实问题。自行构建测试可自动化，但 5 viewport 视觉检查、横屏测试、aria 检查均需人工操作。AFK agent 无法完成这些验证后自判 done。

**建议修复:**
- 方案A: 将 #005 改为 `type: HITL`（人工介入），保留所有验证步骤
- 方案B: 拆分 — AFK issue 仅含 build + test + 自动化检查；HITL issue 含 viewport + 横屏 + 无障碍手动验证

---

### 2. [medium] 全量 issues — 缺失 Risks/Rollback 章节

**问题:** 5 条 issue 均无 `## Risks` 或 `## Rollback` 章节。Plan rubric 要求:
- "已知风险已列出，有缓解措施"
- "有回退方案或止损条件"

**核实结论:** 真实问题。grep 确认 0 匹配。虽然后果低（web-only 改动，git revert 即回退），但 rubric 明确要求应记录。

**建议修复:** 每条 issue 添加至少一行，例如:
```
## 风险
- CSS 变量冲突风险低（新增变量使用 `--mobile-*` 前缀）
- 桌面回归风险中（Sidebar 重构）→ 通过 AC7（零回归）覆盖
- 回退: `git revert` 对应 commit，零数据迁移（web-only）
```

---

### 3. [medium] issues/004-app-layout-integration.md:116 — AC10 无验证手段

**问题:** AC10 "`h-dvh` 回退生效 — Chrome 107 以下使用 `h-screen`" 没有对应的测试或验证手段。测试策略只提到 matchMedia handler 单元测试 + 手动 viewport 检查。

**核实结论:** 真实问题。手动 viewport 无法验证 Chrome 107 行为（没有该环境）。CSS 回退是浏览器行为，目前无机制验证。

**建议修复:** 删除 AC10 或改为 "CSS `h-dvh` 双值回退已实现，在支持 `dvh` 的浏览器上生效"（不承诺旧版本验证）。

---

### 4. [low] issues/005-integration-verification.md:11 — test_files 为空

**问题:** `test_files: []`。宪法规则 #14 要求 "test_files，指向精确的测试文件路径，禁止默认跑全量"。self-check 标注 "N/A（纯手动验证）"。

**核实结论:** 真实问题但严重性低。验证 issue 无测试文件可接受，但应添加注释说明理由，而非留空。

---

### 5. [low] issues/001-css-i18n-store-foundation.md:84 — 架构约束遗漏 R1、R2

**问题:** #001 的架构约束表只引用了 R4、R6，遗漏 R1（不修改 core/server/cli）和 R2（零新依赖）。#003/#004 均引用了 R1+R2。

**核实结论:** 真实问题。虽然后果低（#001 实际未违反 R1/R2），但不符合 "架构约束已引用" 规则，且与同组 issue 不一致。

---

## 二、确认无误的部分

| 检查项 | 结果 |
|--------|------|
| DAG 依赖链无循环 | 001 → (002,003) → 004 → 005，无循环 |
| estimate 全部 ≤1d | 0.5d~1d，合规 |
| AC 可测量性 | 全部 AC 可量化/可断言 |
| 目录已指定 | 每条 issue 有精确代码路径 |
| 前置准备完整 | #001-#005 均列明前置条件 |
| mock/E2E 策略 | 单元测试 + build 验证 + 手动清单 |
| SDK 参考 | 依赖表格完整 |
| 验收无主观 | 全部可量化 |
| Scope 边界 | In/Out 清晰 |
| needs_* 声明 | 全部声明（均为 false） |
| 集成覆盖 | #004 完成端到端连接 |
| 架构约束 | #002-#004 完整引用 |

---

## 三、评审质量总结

| 维度 | 评价 |
|------|------|
| 子进程发现数 | 7 条 (high:1, medium:2, low:2, planChecks: 7) |
| 主进程确认数 | 7 条全部确认，无误报 |
| 误报数 | 0 |
| 主进程补充 | 0 |
| 评审质量 | 良好 — 子进程发现覆盖宪法14项 + plan rubric，每条有 file:line 引用 |

**Overall:** Issues are well-structured but two blocking issues remain: type mismatch in #005 and missing Risks/Rollback across all issues. DAG is clean, ACs are measurable, and the plan is feasible.
