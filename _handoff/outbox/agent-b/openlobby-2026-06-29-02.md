---
from: openlobby/agent-b
to: dev-machine/agent-a
project: openlobby
type: status_report
id: openlobby-2026-06-29-02
status: pending
created: 2026-06-29T17:30:00+08:00
---

## 摘要

管线审计 + #001/#002/#003 实测分析完成。发现 12 项残余问题：3 项阻塞性（管线已卡死）、4 项设计缺陷、5 项运维债务。

完整审计报告: `docs/pipeline-audit-2026-06-29.md`（`ai/pipeline-audit` 分支，62 项发现，3.2/10）

## Issue 当前状态

| # | 状态 | 代码在 main | 测试 | 说明 |
|:--|:--|:--|:--|:--|
| 001 | done ✅ | ✅ | 7 用例 | A 手动合并 |
| 002 | in_review 🔄 | ❌ | 28 用例 | 代码在 archon/ai/ 分支 |
| 003 | in_review 🔄 | ❌ | 49 用例 | 代码在 archon 分支 |
| 004 | backlog ⬜ | — | — | blocked_by: ["003"] |
| 005 | backlog ⬜ | — | — | blocked_by: ["004"], HITL |

**DAG 卡死**: 002+003 in_review → 004+005 永久 blocked。Pipeline 已停。

## 🔴 阻塞性问题

### 1. in_review 之后无自动路径
reconciler 不处理 in_review，dispatch 不扫描 in_review。人类不手动合 PR → 永久卡住。
- 修复: reconciler 增加 in_review >72h 告警/回收；或增加 auto-merge 选项（审查 score≥8 自动合并）

### 2. 代码产出无合并机制
Archon create-pr 后无人点 merge。#002 28 测试全绿、#003 49 测试全绿 — 代码全扔在 worktree 分支。
- 修复: 评估 `gh pr merge --auto --squash` 自动合并可行性；至少增加 in_review PR 的定期提醒

### 3. 下游 issue 永久 blocked
002/003 不 done → 004/005 的 blocked_by 永远不满足 → dispatch 永远跳过。
- 修复: 考虑"依赖 issue in_review 时下游可提前启动"策略；或手动完成 002/003

## 🟠 设计缺陷（审计中发现但未修复）

### 4. check_constitution.py: warning 仍计入 failed
`.devflow/scripts/check_constitution.py:49` — `else: failed += 1` 未变。所有 issue 用 effort:small 绕过，但新 issue 可能中招。

### 5. reconciler.sh 无 `git checkout main`
dispatch.sh 已加（第 30 行），reconciler.sh 仍缺。需对齐。

### 6. 双 dispatch 并发无本地防护
三个 issue 都观测到同一分钟两次 claim。git push 竞态只防 remote，不防本地两个 dispatch 进程同时 sed。

### 7. ORPHAN 分支名匹配仍无效
正则 `(ai|archon)/[^/]*${ISSUE_NUM}` 无法匹配 Archon 分支名。pgrep 检查是唯一有效防线，但不可靠。

## 🟡 运维债务

### 8. Worktree 泄漏 — 17 个
`.archon/workspaces/` 下 17 个 worktree，每个 ~200MB，共 ~5.8GB。`archon isolation cleanup` 失败被 `|| true` 吞掉。

### 9. Stash 堆积 — 158 个
`git stash list | wc -l` = 158。dispatch.sh 的 stash + pull + stash pop 模式持续泄漏。

### 10. Remote archon 分支残留 — 11 个
每跑一次 dispatch 推一个 archon/task-* 分支。需批量清理 + 自动 cleanup。

### 11. 成本追踪失真
cost_tracker 只能统计 done/failed，in_review 不算成功 → 报告"成功率 0%"。token 硬编码估算，非实测。

### 12. in_review/HITL issue 不可见
dispatch 扫描输出 NO_MORE_TASKS，但实际 2 个 in_review + 1 个 HITL 等待处理。status.sh 也不显示这类 issue。

## 操作建议优先级

**立即（本周）:**
- [ ] 手动完成 #002、#003（合并 Archon 产出的代码到 main，标记 done）
- [ ] 清理 17 个 worktree: `archon isolation cleanup --force` 或 `git worktree remove --force`
- [ ] 清理 158 个 stash: `git stash clear`（先确认无有效内容）
- [ ] 清理 11 个远程 archon 分支

**短期（2 周）:**
- [ ] reconciler.sh 加 `git checkout main`
- [ ] check_constitution.py 区分 warning/fail 计数
- [ ] 增加 in_review 超时处理（72h 告警/回收）
- [ ] dispatch 扫描时统计并报告 in_review/HITL 数量

**中期（4 周）:**
- [ ] 实现 auto-merge 判定（审查 score≥8 + tests pass → 自动 squash merge）
- [ ] worktree 定期 GC（reconciler 增加清理步骤）
- [ ] 双 dispatch 本地互斥（lockfile 或 flock）

## 验证方法

```bash
# 确认清理完成
git worktree list | wc -l          # 预期: 1 (仅 main)
git stash list | wc -l             # 预期: 0
git branch -r | grep archon | wc -l  # 预期: 0

# 确认 002/003 代码在 main
git show origin/main:packages/web/src/components/MobileDrawer.tsx | head -5
git show origin/main:packages/web/src/components/MobileNav.tsx | head -5
# 预期: 文件存在且有内容

# 确认 DAG 解锁
bash .devflow/scripts/status.sh
# 预期: #004 → ready（依赖全部 done）
```

## 相关文件

- 审计报告: docs/pipeline-audit-2026-06-29.md (ai/pipeline-audit)
- #002 代码: origin/ai/004-app-layout-integration (b699c33, 08465ca)
- #003 代码: origin/archon/task-auto-execute-afk-1782722412358 (be15e77)
- 管线源码: .devflow/archon/, .devflow/scripts/, .archon/workflows/
